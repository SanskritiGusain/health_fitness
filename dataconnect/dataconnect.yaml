specVersion: "v1beta"
serviceId: "healthfitness"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "healthfitness-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
